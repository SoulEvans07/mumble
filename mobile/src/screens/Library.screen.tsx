import { ReactElement } from 'react';
import { View, StyleSheet } from 'react-native';

interface LibraryScreenProps {}

export function LibraryScreen(props: LibraryScreenProps): ReactElement {
  const {} = props;
  return <View></View>;
}

const styles = StyleSheet.create({});
